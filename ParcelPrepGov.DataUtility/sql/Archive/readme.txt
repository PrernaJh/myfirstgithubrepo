If executed against an empty SQL database, this script will generate the schema for our identity db.

Usually, plugging into existing Identity databases (test or prod) will be sufficient for environment setup.