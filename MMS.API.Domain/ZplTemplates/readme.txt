4/8/2022

We are now tracking the history of our ZPL templates in source control

When there is a change, increment the version and archive the old template

Note: Changes to the CreatePackage.API ZPL templates must be made in app.settings for both MMS.CreatePackage.API and PackageTracker.API

